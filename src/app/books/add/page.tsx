import styles from './page.module.css'

const AddBookPage = () => {
    return (
        <main className={styles.container}>
            add book
        </main>
    );
};

export default AddBookPage;
